
@{
    ViewData["Title"] = "Restaurant tables";
    Employee? loggedInEmployee = (Employee?)ViewData["LoggedInEmployee"]; 

}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="mb-0">Restaurant Tables</h2>
        <div>
            <a href="/RestaurantTable/Create" class="btn btn-primary me-2">Create New Table</a>
            <button class="btn btn-outline-secondary" onclick="location.reload();">Refresh</button>
        </div>
    </div>

    <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4 g-4">
        <partial name="_RestaurantTables"/>
    </div>
</div>